{
  "name": "failed-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "cd bdg && npm install && node -e \"const fs=require('fs'); ['node_modules/.bin/puppeteer','node_modules/.bin/playwright','node_modules/.bin/browsers'].forEach((p)=>{try{fs.chmodSync(p, 0o755);}catch(e){}})\"",
    "start": "cd bdg && npm start",
    "build": "cd bdg && npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "puppeteer": "^25.1.0"
  }
}
